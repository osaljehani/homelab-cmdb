"""MCP (Model Context Protocol) server exposing the CMDB domain layer as tools.

A thin shell over ``cmdb.domain.services`` alongside the CLI and Web shells. See
``server.py`` for the FastMCP instance and ``schemas.py`` for the output models.
"""
