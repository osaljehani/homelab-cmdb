__pycache__/
*.pyc
*.pyo
.venv/
dist/
*.egg-info/
.pytest_cache/
*.db
# Trivy scan JSONs imported by the image-scan pipeline — real homelab vuln data
data/scans/
# cmdb export snapshots — whole-DB dumps of real homelab data
data/exports/
.superpowers/
CLAUDE.md
.remember/
out/
tests/fixtures/
# Real, homelab-data-bearing tests stay local; only scrubbed *-example.py copies are published.
tests/test_*.py
!tests/test_*-example.py
docs/superpowers/
inventory/
.env
data/*.bak*
# Local, site-specific deployment override (bind address / ports). Never publish.
docker-compose.override.yml
# Auto-generated session signing key (see Settings.resolved_secret_key). Never commit.
# No leading path: it is written beside the DB, and CMDB_DB_PATH defaults to the
# repo root, so it does not always land under data/.
.secret_key
